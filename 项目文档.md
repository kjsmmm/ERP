# 工厂 ERP 系统 - 项目规划文档

> 版本: v1.0 | 日期: 2026-05-09 | 状态: 规划阶段

---

## 一、项目概述

### 1.1 项目目标

建设一套企业级工厂 ERP 系统，覆盖从客户管理、产品管理、订单管理到生产、采购、库存、质量管理的全流程，实现工厂核心业务的数字化管理。

### 1.2 业务背景

- **工厂模式**: 混合模式（OEM/ODM 代工 + 自有产品制造）
- **工厂数量**: 单工厂运营（架构预留多工厂扩展能力）
- **仓库数量**: 多仓库管理
- **系统规模**: 中型（20-100 用户）
- **外部集成**: 暂无（后续可扩展）
- **移动端**: 暂不考虑
- **多语言**: 不需要（纯中文）

### 1.3 核心原则

- **安全优先**: 认证授权、数据加密、审计追踪，企业级安全标准
- **稳固落地**: 不追求技术时髦，选择成熟稳定的技术栈
- **渐进交付**: 纵向切片迭代，每次交付可用功能
- **预留扩展**: 数据模型和架构设计为未来多工厂、多地点留好接口

---

## 二、技术架构

### 2.1 技术栈总览

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **前端框架** | Vue 3.3+ (Composition API) | 响应式框架，模板语法直觉友好 |
| **UI 组件库** | Element Plus | 企业级组件丰富，表格/表单成熟 |
| **管理后台** | Vben Admin (vue-vben-admin) | 开箱即用的企业管理后台方案 |
| **构建工具** | Vite 5 | 极速构建，开发体验好 |
| **状态管理** | Pinia | Vue 3 官方推荐状态管理 |
| **HTTP 客户端** | Axios | 请求拦截、响应处理、Token 刷新 |
| **图表** | ECharts | 数据可视化 |
| **类型系统** | TypeScript | 强类型，减少运行时错误 |
| **后端框架** | Spring Boot 3.2+ | Java 生态企业级首选 |
| **JDK** | JDK 17 (LTS) | 长期支持版本 |
| **安全框架** | Spring Security 6 + JWT | 认证授权标准方案 |
| **ORM** | MyBatis-Plus | 简化数据库操作，内置分页/代码生成 |
| **工作流引擎** | Flowable 7.0+ | BPMN 2.0 标准，企业级工作流 |
| **接口文档** | SpringDoc (OpenAPI 3) | 自动生成 API 文档 |
| **构建工具** | Maven | 依赖管理和构建 |
| **数据库** | MySQL 8.0+ | InnoDB 引擎，utf8mb4 字符集 |
| **缓存** | Redis 7+ | 会话缓存、权限缓存、限流 |
| **文件存储** | 本地存储 | 可扩展至 MinIO/阿里云 OSS |
| **容器化** | Docker + Docker Compose | 一致性部署环境 |
| **反向代理** | Nginx | SSL 终止、静态资源、负载均衡 |

### 2.2 架构设计

#### 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户层                                │
│              浏览器 (Chrome / Edge / Firefox)                │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTPS
┌──────────────────────────▼──────────────────────────────────┐
│                     Nginx 反向代理                           │
│              SSL 终止 / 静态资源 / 负载均衡                  │
└────────┬────────────────────────────────────────┬────────────┘
         │                                        │
         ▼                                        ▼
┌─────────────────┐                    ┌─────────────────────┐
│   Vue 3 前端    │                    │   Spring Boot 后端  │
│   (静态资源)    │                    │   (REST API)        │
└─────────────────┘                    └──────────┬──────────┘
                                                  │
                              ┌───────────────────┼───────────────────┐
                              ▼                   ▼                   ▼
                       ┌────────────┐      ┌────────────┐     ┌────────────┐
                       │   MySQL    │      │   Redis    │     │  文件存储  │
                       │   主数据库 │      │   缓存     │     │  MinIO/本地│
                       └────────────┘      └────────────┘     └────────────┘
```

#### 后端分层架构

```
┌─────────────────────────────────────────────────────────────┐
│  Controller 层   │ 接口定义、参数校验、统一响应              │
├──────────────────┼──────────────────────────────────────────┤
│  Service 层      │ 业务逻辑、事务管理、领域规则              │
├──────────────────┼──────────────────────────────────────────┤
│  Mapper 层       │ 数据访问、MyBatis-Plus、分页查询          │
├──────────────────┼──────────────────────────────────────────┤
│  Entity 层       │ 数据库实体映射                            │
└─────────────────────────────────────────────────────────────┘

横切关注点:
├── Spring Security + JWT    (认证授权)
├── Flowable                 (工作流引擎)
├── AOP 日志                 (操作审计)
├── 全局异常处理             (统一错误响应)
└── Redis 缓存               (性能优化)
```

#### 模块化单体架构

采用模块化单体（Modular Monolith）设计，内部模块边界清晰，外部单一部署：

```
com.erp
├── common/                    # 公共模块
│   ├── config/                # 全局配置
│   ├── security/              # 安全组件
│   ├── exception/             # 异常处理
│   ├── annotation/            # 自定义注解
│   └── utils/                 # 工具类
│
├── module/                    # 业务模块
│   ├── auth/                  # 认证模块
│   ├── system/                # 系统管理（用户/角色/部门/日志）
│   ├── customer/              # 客户管理
│   ├── product/               # 产品管理（含 BOM）
│   ├── order/                 # 订单管理
│   ├── inventory/             # 库存管理
│   ├── production/            # 生产管理
│   ├── purchase/              # 采购管理
│   ├── quality/               # 质量管理
│   └── workflow/              # 工作流模块（Flowable 集成）
│
└── ErpApplication.java        # 启动类
```

每个业务模块内部结构:

```
module/customer/
├── controller/                # 接口层
├── service/                   # 业务层
│   ├── impl/
├── mapper/                    # 数据访问层
├── entity/                    # 数据库实体
├── dto/                       # 请求/响应对象
├── vo/                        # 视图对象
└── enums/                     # 枚举常量
```

---

## 三、安全架构

### 3.1 认证方案

```
┌─────────────────────────────────────────────────────────────┐
│                    认证流程                                  │
└─────────────────────────────────────────────────────────────┘

用户登录请求
     │
     ▼
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  验证码   │───▶│ 密码校验 │───▶│ 签发JWT  │───▶│ 返回Token│
│  校验    │    │ BCrypt   │    │          │    │          │
└──────────┘    └──────────┘    └──────────┘    └──────────┘

Token 设计:
├── Access Token:  有效期 15 分钟，用于接口认证
├── Refresh Token: 有效期 7 天，用于刷新 Access Token
└── 存储方式:      Redis 存储活跃 Token，支持主动注销

安全策略:
├── 密码: BCrypt 哈希 + 随机盐值
├── 登录失败: 5 次失败锁定 30 分钟
├── 验证码: 图形验证码 / 行为验证码
└── 异地登录: IP 变化时告警通知
```

### 3.2 授权方案 (RBAC)

```
┌─────────────────────────────────────────────────────────────┐
│                    RBAC 权限模型                             │
└─────────────────────────────────────────────────────────────┘

用户(User) ──M:N──▶ 角色(Role) ──M:N──▶ 权限(Permission)

权限维度:
├── 菜单权限: 控制用户能看到哪些页面
├── 按钮权限: 控制用户能执行哪些操作（新增/编辑/删除/导出）
└── 数据权限: 控制用户能看哪些部门的数据
    ├── 全部数据
    ├── 本部门及下级部门数据
    ├── 本部门数据
    └── 仅本人数据

预设角色:
├── 系统管理员: 全部权限
├── 厂长/总经理: 全部数据查看 + 审批权限
├── 销售主管: 客户/订单管理 + 本部门数据
├── 车间主管: 生产管理 + 本车间数据
├── 仓库管理员: 库存管理 + 全部库存数据
├── 采购员: 采购管理 + 本人数据
├── 质检员: 质量管理 + 检验操作
└── 普通员工: 基础查看权限
```

### 3.3 审计追踪

| 审计类型 | 记录内容 | 保留期限 |
|----------|----------|----------|
| 操作日志 | 操作人、操作时间、操作类型、IP 地址、请求参数、响应结果 | 1 年 |
| 登录日志 | 用户名、登录时间、IP 地址、设备信息、登录结果 | 1 年 |
| 数据变更 | 变更人、变更时间、变更表、变更字段、旧值、新值 | 3 年 |
| 审批日志 | 审批人、审批时间、审批动作、审批意见 | 3 年 |

### 3.4 传输与存储安全

- **传输层**: HTTPS 强制（TLS 1.2+）
- **存储层**: 敏感字段（密码、身份证、银行卡）加密存储
- **接口防护**: SQL 注入防护（参数化查询）、XSS 防护（输出编码）、CSRF 防护（Token 验证）
- **限流策略**: 接口级别限流，防暴力破解和 CC 攻击

---

## 四、功能模块

### 4.1 模块优先级

| 优先级 | 模块 | 说明 |
|--------|------|------|
| **P0** | 用户认证与权限 | 系统基础，没有它系统无法运行 |
| **P0** | 客户管理 | 销售业务起点 |
| **P0** | 产品管理（含 BOM） | 制造业核心，影响生产和成本 |
| **P0** | 订单管理 | 业务主流程 |
| **P0** | 库存管理 | 物料和成品管控 |
| **P1** | 生产管理 | 工厂核心业务 |
| **P1** | 采购管理 | 供应链保障 |
| **P1** | 质量管理 | 产品品质保证 |
| **P1** | 报表与分析 | 经营决策支持 |
| **P2** | 财务管理 | 应收应付、成本核算（后续迭代） |
| **P2** | 设备管理 | 设备台账、维护计划（后续迭代） |

### 4.2 用户认证与权限

**页面:**
- 登录页（用户名 + 密码 + 验证码）
- 用户管理（增删改查、重置密码、启用/禁用）
- 角色管理（角色列表、分配权限）
- 部门管理（组织架构树）
- 个人中心（修改密码、查看登录记录）
- 操作日志（审计追踪）

**核心接口:**
```
POST   /api/auth/login          # 登录
POST   /api/auth/logout         # 注销
POST   /api/auth/refresh        # 刷新 Token
GET    /api/users               # 用户列表
POST   /api/users               # 创建用户
PUT    /api/users/{id}          # 更新用户
DELETE /api/users/{id}          # 删除用户
GET    /api/roles               # 角色列表
POST   /api/roles               # 创建角色
PUT    /api/roles/{id}          # 更新角色
GET    /api/departments         # 部门列表
POST   /api/departments         # 创建部门
GET    /api/logs                # 操作日志
```

### 4.3 客户管理

**页面:**
- 客户列表（搜索、筛选、分页）
- 客户详情（基本信息 + 联系人 + 历史订单 + 合同）
- 客户分类（客户等级、行业）
- 联系人管理（一个客户多个联系人）
- 客户跟进记录

**核心数据:**
- 客户编码、名称、类型（国内/国外）
- 客户等级、行业、地址
- 税号、开户行、账期、信用额度
- 联系人：姓名、职位、电话、邮箱

### 4.4 产品管理

**页面:**
- 产品列表（搜索、分类筛选）
- 产品详情（规格参数 + BOM + 图片）
- 产品分类管理
- BOM 物料清单管理（产品由哪些原材料组成）
- 产品价格管理（标准售价、阶梯报价）

**核心数据:**
- 产品编码、名称、分类、规格型号
- 单位、重量、标准成本、标准售价
- BOM：物料 + 用量 + 损耗率

**BOM 结构示例:**
```
成品 A
├── 原材料 1 × 2 个 (损耗率 1%)
├── 原材料 2 × 1 个 (损耗率 0.5%)
├── 半成品 B × 1 个
│   ├── 原材料 3 × 3 个
│   └── 原材料 4 × 1 个
└── 包装材料 × 1 套
```

### 4.5 订单管理

**页面:**
- 订单列表（按状态筛选：待确认/生产中/已完成/已取消）
- 订单创建（选客户 → 选产品 → 填数量/单价/交期）
- 订单详情（订单信息 + 产品明细 + 生产进度 + 发货记录）
- 订单变更（修改数量/交期，需要审批）
- 订单跟踪（状态流转可视化）

**订单状态机:**
```
草稿 ──▶ 已确认 ──▶ 生产中 ──▶ 已完成 ──▶ 已关闭
 │          │          │
 ▼          ▼          ▼
已取消    已取消     已暂停
```

### 4.6 库存管理

**页面:**
- 库存总览（各仓库库存汇总、库存预警）
- 入库管理（采购入库、生产入库、退货入库）
- 出库管理（销售出库、生产领料、报废出库）
- 库存盘点（创建盘点单 → 实盘 → 差异处理）
- 库存查询（按产品/仓库/批次查库存）
- 仓库管理（仓库信息、库位管理）
- 库间调拨（仓库间物料转移）

**核心数据:**
- 产品 ID、仓库 ID、批次号、数量、库位
- 安全库存、最高库存、最低库存
- 最后入库时间、最后出库时间

### 4.7 生产管理

**页面:**
- 生产计划（从订单生成生产计划）
- 工单管理（生产工单的创建、下达、完工）
- 工艺路线（定义产品的生产工序）
- 生产排程（甘特图排产）
- 生产报工（工人汇报完成数量）
- 生产进度跟踪

**核心流程:**
```
销售订单 ──▶ 生产计划 ──▶ 生产工单 ──▶ 领料 ──▶ 生产 ──▶ 质检 ──▶ 入库
```

**工艺路线示例:**
```
产品 A 的工艺路线:
工序 1: 下料 (设备: 切割机, 标准工时: 10min)
    ↓
工序 2: 折弯 (设备: 折弯机, 标准工时: 15min)
    ↓
工序 3: 焊接 (设备: 焊机, 标准工时: 20min)
    ↓
工序 4: 喷涂 (设备: 喷涂线, 标准工时: 30min)
    ↓
工序 5: 组装 (人工, 标准工时: 25min)
    ↓
工序 6: 检验 (人工, 标准工时: 10min)
```

### 4.8 采购管理

**页面:**
- 供应商管理（供应商档案、资质、评估）
- 采购申请（需求提交、审批）
- 采购订单（询价、比价、下单）
- 采购入库（到货检验、入库）
- 采购对账（与供应商对账）

**核心流程:**
```
采购申请 ──▶ 审批 ──▶ 采购订单 ──▶ 到货 ──▶ IQC检验 ──▶ 入库 ──▶ 对账
```

### 4.9 质量管理

**页面:**
- 检验标准管理（定义各类产品的检验项目和标准）
- 来料检验 (IQC)：采购物料入库前检验
- 过程检验 (IPQC)：生产过程中检验
- 成品检验 (OQC)：成品出货前检验
- 不合格品处理（让步接收/返工/报废）
- 质量统计分析（合格率趋势、不良原因分析）

### 4.10 报表与分析

- 销售报表（销售额、客户排名、产品排名）
- 生产报表（产量、良品率、设备利用率）
- 库存报表（库存周转率、呆滞物料、预警）
- 采购报表（采购金额、供应商排名）
- 质量报表（合格率趋势、不良 Pareto 分析）
- 经营看板（核心 KPI 汇总展示）

---

## 五、工作流设计

### 5.1 集成策略

采用**分阶段集成**策略：

**第一阶段：轻度集成**
- Flowable 只负责审批流（采购审批、订单变更审批等）
- 业务状态由代码管理（订单状态、库存变动等）
- 目标：先把核心业务跑起来

**第二阶段：深度集成（系统稳定后）**
- 将需要灵活配置的业务流程交给 Flowable
- 如：生产工单流转、质检流程
- 平滑迁移，无需重写

### 5.2 审批场景

| 审批场景 | 触发条件 | 审批链 |
|----------|----------|--------|
| 采购申请审批 | 提交采购申请 | 申请人 → 部门主管 → 厂长（金额>5000） |
| 采购订单审批 | 创建采购订单 | 采购员 → 采购主管 → 财务确认 |
| 订单变更审批 | 修改已确认订单 | 销售员 → 销售主管 → 生产确认 |
| 不合格品处理 | 质检不合格 | 质检员 → 质量主管 → 技术判定 |
| 费用报销 | 提交报销单 | 申请人 → 部门主管 → 财务 |

---

## 六、数据库设计要点

### 6.1 多工厂扩展预留

所有核心表包含以下字段，为未来多工厂扩展做准备：

```sql
factory_id    BIGINT    COMMENT '工厂ID，当前默认为1'
```

当前单工厂场景下，`factory_id` 默认为 1，所有查询不感知该字段。未来扩展多工厂时，只需：
1. 增加工厂表
2. 在查询条件中增加 `factory_id` 过滤
3. 增加工厂间数据隔离逻辑

### 6.2 通用字段

所有业务表包含以下通用字段：

```sql
id            BIGINT       PRIMARY KEY AUTO_INCREMENT
created_by    BIGINT       COMMENT '创建人ID'
created_at    DATETIME     COMMENT '创建时间'
updated_by    BIGINT       COMMENT '更新人ID'
updated_at    DATETIME     COMMENT '更新时间'
deleted       TINYINT      DEFAULT 0 COMMENT '逻辑删除标记'
factory_id    BIGINT       DEFAULT 1 COMMENT '工厂ID'
remark        VARCHAR(500) COMMENT '备注'
```

### 6.3 核心表结构概览

| 模块 | 核心表 | 说明 |
|------|--------|------|
| 系统 | sys_user, sys_role, sys_permission, sys_dept, sys_log | 用户/角色/权限/部门/日志 |
| 客户 | customer, customer_contact, customer_follow | 客户/联系人/跟进记录 |
| 产品 | product, product_category, product_bom, bom_item | 产品/分类/BOM/物料明细 |
| 订单 | sales_order, order_item | 销售订单/订单明细 |
| 库存 | inventory, warehouse, stock_in, stock_out, stock_check | 库存/仓库/入库/出库/盘点 |
| 生产 | production_order, process_route, process_step, work_report | 生产工单/工艺路线/工序/报工 |
| 采购 | supplier, purchase_request, purchase_order, purchase_item | 供应商/采购申请/采购订单/明细 |
| 质量 | quality_standard, inspection_record, defect_record | 检验标准/检验记录/不良记录 |

---

## 七、迭代计划

### 迭代策略：纵向切片

每次迭代交付一个完整可用的功能模块，好处：
- 早期验证：第一个模块做完即可投入使用，边用边发现问题
- 经验积累：第一个模块的开发模式成为后续模块的模板
- 问题早暴露：架构问题在第一个迭代就能发现

### 迭代明细

| 迭代 | 内容 | 预计周期 | 交付物 |
|------|------|----------|--------|
| **迭代 0** | 基础设施搭建 | 1-2 周 | 项目骨架、开发环境、框架跑通 |
| **迭代 1** | 用户认证 + 权限 + 客户管理 | 2-3 周 | 可登录、可管理客户的系统 |
| **迭代 2** | 产品管理 + BOM | 2-3 周 | 可管理产品和物料清单 |
| **迭代 3** | 订单管理 + 库存基础 | 2-3 周 | 可下单、可查看库存 |
| **迭代 4** | 生产管理 | 2-3 周 | 可从订单生成工单、跟踪生产 |
| **迭代 5** | 采购管理 + 质量管理 | 2-3 周 | 采购和质检闭环 |
| **迭代 6** | 报表 + 工作流 + 收尾 | 2-3 周 | 完整可用的 ERP 系统 |

**总计: 约 14-20 周（3.5-5 个月）**

### 迭代 0 详细任务

- [ ] 前端项目初始化（基于 Vben Admin）
- [ ] 后端项目初始化（Spring Boot 3 脚手架）
- [ ] 数据库初始化（MySQL + 建表脚本）
- [ ] Docker 开发环境（docker-compose.yml）
- [ ] 统一响应格式（Result<T>）
- [ ] 全局异常处理
- [ ] 日志框架配置（SLF4J + Logback）
- [ ] 跨域配置
- [ ] 接口文档配置（SpringDoc）
- [ ] 前后端联调验证

---

## 八、部署方案

### 8.1 开发环境

```yaml
# docker-compose.yml
services:
  mysql:
    image: mysql:8.0
    ports: ["3306:3306"]
    environment:
      MYSQL_ROOT_PASSWORD: xxx
      MYSQL_DATABASE: erp

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
```

### 8.2 生产环境

```
┌─────────────────────────────────────────────────────────────┐
│                    云服务器 (4核8G 起步)                     │
│                                                             │
│  ┌─────────┐  ┌─────────────┐  ┌─────────┐  ┌───────────┐ │
│  │  Nginx  │  │ Spring Boot │  │  MySQL  │  │   Redis   │ │
│  │  :80/443│  │   :8080     │  │  :3306  │  │   :6379   │ │
│  └─────────┘  └─────────────┘  └─────────┘  └───────────┘ │
│                                                             │
│  Docker Compose 编排，一键部署                               │
└─────────────────────────────────────────────────────────────┘
```

### 8.3 备份策略

| 备份对象 | 频率 | 保留期限 | 方式 |
|----------|------|----------|------|
| MySQL 数据库 | 每日凌晨 | 30 天 | mysqldump + 云快照 |
| 上传文件 | 每周 | 90 天 | 增量备份 |
| 应用配置 | 每次变更 | 永久 | Git 版本管理 |

---

## 九、项目结构

```
ERP-OpenSpec/
├── erp-frontend/                  # 前端项目
│   ├── src/
│   │   ├── api/                   # 接口定义
│   │   ├── views/
│   │   │   ├── auth/              # 登录/权限
│   │   │   ├── customer/          # 客户管理
│   │   │   ├── product/           # 产品管理
│   │   │   ├── order/             # 订单管理
│   │   │   ├── inventory/         # 库存管理
│   │   │   ├── production/        # 生产管理
│   │   │   ├── purchase/          # 采购管理
│   │   │   ├── quality/           # 质量管理
│   │   │   ├── report/            # 报表分析
│   │   │   └── system/            # 系统管理
│   │   ├── router/                # 路由
│   │   ├── store/                 # 状态管理
│   │   ├── components/            # 公共组件
│   │   └── utils/                 # 工具函数
│   ├── package.json
│   └── vite.config.ts
│
├── erp-backend/                   # 后端项目
│   ├── src/main/java/com/erp/
│   │   ├── common/                # 公共模块
│   │   ├── module/                # 业务模块
│   │   └── ErpApplication.java
│   ├── src/main/resources/
│   │   ├── application.yml
│   │   └── mapper/                # MyBatis XML
│   └── pom.xml
│
├── docker/                        # Docker 配置
│   ├── docker-compose.yml
│   ├── docker-compose.prod.yml
│   ├── nginx/
│   │   └── nginx.conf
│   └── mysql/
│       └── init.sql
│
├── docs/                          # 项目文档
│   └── 项目文档.md
│
├── .gitignore
└── README.md
```

---

## 十、风险与应对

| 风险 | 影响 | 应对策略 |
|------|------|----------|
| 前端经验不足 | 开发效率低、代码质量差 | 使用 Vben Admin 脚手架，第一个模块重点打磨，形成模板 |
| Flowable 学习曲线 | 工作流集成进度慢 | 第一阶段轻度集成，降低复杂度 |
| 需求频繁变更 | 迭代计划被打乱 | 纵向切片交付，每个迭代可独立调整 |
| 数据库设计不合理 | 后期重构成本高 | 迭代 0 重点设计核心表结构，预留扩展字段 |
| 性能瓶颈 | 用户体验差 | Redis 缓存热点数据，SQL 索引优化，分页查询 |

---

*文档维护: 随项目演进持续更新*
